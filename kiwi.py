class Node:
	"""Time and place of traveler"""
	def __init__(self, place, time):
		self.place = place
		self.time = time


def bfs( Node from, Node too,Node[] history):
	if from.code == too.code:

	for next in next_stop(Node from):
		pass
